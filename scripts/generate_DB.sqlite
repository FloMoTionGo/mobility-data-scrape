-- generate VBB Table
CREATE TABLE IF NOT EXISTS vbb (
   id varchar(255),
   arrival TIMESTAMP,
   latitude varchar(255),
   longitude varchar(255)
   );

CREATE INDEX IF NOT EXISTS i_arrival
   ON vbb(arrival);

-- generate TIER table
